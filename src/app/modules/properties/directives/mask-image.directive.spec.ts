import { MaskImageDirective } from './mask-image.directive';

describe('MaskImageDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskImageDirective();
    expect(directive).toBeTruthy();
  });
});
